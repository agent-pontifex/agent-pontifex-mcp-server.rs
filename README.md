# Deprecated: moved to `agent-pontifex/ap-mcp-server.rs`

This repository is retained for historical provenance. Active development has moved to [`agent-pontifex/ap-mcp-server.rs`](https://github.com/agent-pontifex/ap-mcp-server.rs).

The full-name repository contained only repository guidance and a stub README; the canonical short-name repository contains the actual Rust package, CI, Zed manifest, configuration boundary, and MCP service scaffold. No unique MCP implementation remains only here.

Do not add source, releases, packages, generated artifacts, or automation here. Open all new MCP work against `agent-pontifex/ap-mcp-server.rs`.
